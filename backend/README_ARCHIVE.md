# ARCHIVED: Legacy backend from tip_scholarsphere

This folder is retained for historical reference only. All active backend development is now in the root `app/` and `database/` folders.

- Do not add new code here.
- Use `app/` for all FastAPI backend work.
- Use `database/` for database logic and migrations.

If you need to restore code from this folder, copy it into the main backend structure and update imports accordingly.
